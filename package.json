{
	"name": "ats-email-notifications",
	"version": "1.0.1",
	"description": "",
	"scripts": {
		"test": "echo \"Error: no test specified\" && exit 1",
		"start": "nodemon",
		"build": "tsc",
		"clean": "rm -rf ./build",
		"postinstall": "npm run build",
		"postversion": "git push && git push --tags"
	},
	"author": "",
	"license": "ISC",
	"dependencies": {
		"@types/minimist": "^1.2.0",
		"@types/node": "^13.9.0",
		"@types/nodemailer": "^6.4.0",
		"dotenv": "^8.2.0",
		"minimist": "^1.2.4",
		"moment": "^2.24.0",
		"nodemailer": "^6.4.5"
	},
	"devDependencies": {
		"nodemon": "^2.0.2"
	},
	"bin": {
		"fleetrun-overdue-email-report": "./build/bin/fleetrun-overdue-email-report.js"
	}
}
